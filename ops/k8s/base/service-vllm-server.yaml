apiVersion: v1
kind: Service
metadata:
  name: vllm-server
  namespace: medical-kg
  labels:
    app: vllm-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: vllm-server

apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen3
  labels:
    app: embeddings
spec:
  type: ClusterIP
  selector:
    app: embeddings
  ports:
    - name: http
      port: 8001
      targetPort: 8001
